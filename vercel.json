{
  "buildCommand": "cd geo-monitor-v2 && npm install && npm run build",
  "outputDirectory": "geo-monitor-v2/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
