# TODO — July 14, 2026

## Bugs to Fix

### 1. Data Fetching Is Bugged
- [ ] Investigate broken data fetch — identify which endpoints/data sources are failing
- [ ] Check Supabase REST calls in `src/lib/supabase-geo.js` for errors
- [ ] Verify GeoJSON loading from `public/geoJSON/` for boundary/watershed files
- [ ] Check network tab for failed requests on both Watersheds and Boundaries modes
- [ ] Fix fetch errors so data loads reliably

### 2. Reset to Default — Side Panel Logic
- [ ] Review `_resetSubWatershedDefaults()` in `src/lib/hydro-mode.js`
- [ ] Review Reset button in `src/components/OverlayPanel.jsx` and `src/lib/app.js`
- [ ] Ensure reset properly restores all overlay styles (fill opacity, outline color/opacity/weight, stream order, slope, LCM)
- [ ] Ensure reset clears basin-level customizations and reverts to config defaults
- [ ] Verify reset works from both the side panel and overlay controls

### 3. Basin Tooltip Labels — Revert to July 8 Behavior
- [ ] Check git history for tooltip/hover behavior as of July 8 (`git log --since="2025-07-07" --until="2025-07-09"`)
- [ ] The basin label tooltip (e.g. Agno and others) was changed — revert to the original hover behavior
- [ ] Review `_updateHydroLabels()` in `src/lib/hydro-mode.js` and the `.basin-label` CSS in `src/assets/css/map.css`
- [ ] Ensure basin labels show on hover (not permanent), matching the July 8 state

## Performance Improvements

### 4. Map Page Load Performance
- [ ] Audit initial load — identify bottlenecks (GeoJSON parsing, layer rendering, network requests)
- [ ] Consider lazy-loading GeoJSON data (only fetch when mode is activated)
- [ ] Consider code-splitting the map page bundle (`React.lazy` + `Suspense`)
- [ ] Review `src/lib/map-layers.js` `initLayers()` — parallel fetching is good but check for sequential waits
- [ ] Check if large GeoJSON files (watersheds, boundaries) can be fetched progressively
- [ ] Review PMTiles/vector tile loading — ensure tiles are cached and not re-fetched
- [ ] Profile the Leaflet initialization — minimize DOM operations before first render
- [ ] **Do NOT alter existing data files** — find alternatives to optimize loading around them

### 5. UI/UX Improvements — Map Page
- [ ] Review overall map layout and controls positioning
- [ ] Improve loading states and transitions
- [ ] Polish side panel design and interactions
- [ ] Review bottom bar controls usability
- [ ] Ensure responsive design works well on mobile/tablet
- [ ] Review color scheme and contrast for accessibility
- [ ] Check overlay panel design and toggle controls
- [ ] Improve hover states and click feedback on map features

### 6. UI/UX Improvements — Landing Page
- [ ] Review `src/components/LandingPage.jsx` and `public/assets/js/script.js`
- [ ] Improve hero section design and copy
- [ ] Review river-basin gallery layout and responsiveness
- [ ] Polish scroll animations (Lenis smooth scroll)
- [ ] Ensure CTA buttons are clear and prominent
- [ ] Review typography and spacing
- [ ] Check mobile responsiveness of landing page
- [ ] Improve stats/counter section design

---

## Priority Order
1. **Bug fixes** (1, 2, 3) — unblock core functionality
2. **Performance** (4) — user experience depends on fast load
3. **UI/UX** (5, 6) — polish after functionality is solid
